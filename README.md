

## RUN
docker-compose up -d

## Checkout
```bash
git clone ...
git submodule init
git submodule update 
```

## Changing branch
```bash
git checkout development 
git submodule update --init --recursive --remote
```
